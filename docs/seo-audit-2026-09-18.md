# SEO-Arbeitsstand – 18. September 2026

Nachtrag: Die nachfolgenden historischen Messungen werden durch
`seo-pr-checklist-2026-09-18.md` ergänzt. Sichtbare Breadcrumbs und zusätzliche
Formularschutzmaßnahmen sind inzwischen beauftragt und umgesetzt; offene
Freigabe-/Deployment-Punkte stehen in `../SEO-TODO.md`.

## Umfang und Freigabestand

Lokale Änderungen auf `codex/medivium-partner`, Ausgangsstand `7f427d5`.
Nicht gepusht oder veröffentlicht. Teil A ist **teilweise abgeschlossen**;
Teil B wurde gemäß der geforderten Reihenfolge noch nicht begonnen.
Nachfolgende Nutzervorgaben und sämtliche offenen Punkte stehen ausschließlich
in `../SEO-TODO.md`. Der folgende Bericht dokumentiert die technischen Tests,
nicht eine medizinische oder juristische Abnahme.

## Teil A

- **A1 umgesetzt:** Das Locale-Layout ist das Root-Layout und liefert `html lang`
  bereits serverseitig. Die nachträgliche Client-Korrektur wurde entfernt.
- **A2 umgesetzt:** `i18n/paths.ts` und `lib/seo.ts` erzeugen übersetzte Pfade,
  Canonical und vier hreflang-Alternativen einschließlich Selbstreferenz und
  x-default. Metadaten stehen für alle Clients im initialen Head. Die nicht
  zu den Patientenslugs passenden automatischen HTTP-Link-Alternativen von
  next-intl sind deaktiviert; Sprachumschaltung verwendet dieselbe Pfadfunktion.
- **A3 lokal geprüft:** `next.config.ts` leitet den www-Host mit genau 301 auf
  den Haupt-Host um. Pfad und Query bleiben erhalten. Der Live-Host muss nach
  einer gesondert freigegebenen Veröffentlichung erneut geprüft werden.
- **A4 umgesetzt:** Netzwerkseite mit vollständiger lokalisierter Metadata,
  Open Graph und Twitter Card; derselbe Helper gilt für alle aktiven Seiten.
- **A5 umgesetzt:** Beschreibende Bildtexte in DE/EN/ES, dekorative Bilder
  ausdrücklich mit leerem Alt und `aria-hidden`. Alle Bilder der 27 Seiten
  haben Alt-Attribute sowie Abmessungen beziehungsweise Fill und Sizes.
- **A6 geprüft, Annahme im Auftrag nicht bestätigt:** Das × ist ein bereits
  `aria-hidden` markiertes Partnerschaftszeichen, kein Lightbox-Schließen-Button.
  Kein interaktives Element in den Überschriften der geprüften Seiten.
- **A7 verbessert:** Hero-Bilder waren bereits priorisiert und nicht lazy.
  Explizites `fetchPriority="high"` ergänzt; responsive Sizes für Systembilder,
  Praxisbilder und Logos vermeiden unnötig große Downloads. Keine Änderung an CSS,
  Positionen oder sichtbaren Bildabmessungen.
- **A8 umgesetzt:** 27 statt 15 Sitemap-URLs, einschließlich aller sechs
  freigeschalteten Praxisprofile in drei Sprachen. Praxis-URLs werden aus
  `content/clinics.ts` abgeleitet. `lastmod` ist das tatsächliche Datum dieser
  Metadatenänderung, nicht die Build-Zeit. Künftige inhaltliche Änderungen
  müssen die jeweiligen Datumsangaben aktualisieren. Weiterleitungsziele,
  Fehlerseiten und nicht freigeschaltete Profile sind nicht in der Sitemap.
- **A9 umgesetzt:** Titles maximal 60 Zeichen und Marke genau einmal;
  Descriptions 140–160 Zeichen, lokalisiert mit Handlungsaufforderung. Der
  im Auftrag vorgeschlagene deutsche Patiententitel überschreitet 60 Zeichen
  und wurde deshalb gekürzt. Der ausdrücklich beizubehaltende Startseitentitel
  bleibt eine Ausnahme von der Marken-Suffix-Reihenfolge.
- **A10 teilweise umgesetzt:** Organization mit stabiler ID, Gebiet,
  Themenfeldern und Kontakt; WebSite je Sprache; BreadcrumbList auf allen
  aktiven Unterseiten; bestehende Praxistypen beibehalten, Geo aus Bestandsdaten
  und memberOf ergänzt. Keine erfundenen Öffnungszeiten, Social-Profile oder
  medizinischen Leistungen. Kein Schema für unsichtbare FAQs. Unbestätigte
  Eigenschaften werden weggelassen; leere Schemawerte werden nicht serialisiert.
- **A11 umgesetzt:** Robots und Googlebot einschließlich großer Bildvorschau;
  404-Seiten liefern noindex; robots.txt verweist auf die korrekte Sitemap.
- **A12 durch Nutzer ersetzt:** Keine eigenen Rechtstextseiten anlegen;
  Footer-Ziele bleiben ausdrücklich bei Cell Education.

## Prüfungen

```sh
npm run build -- --webpack
npm run start -- --port 3012
node scripts/audit-seo.mjs http://localhost:3012
```

- Produktions-Build einschließlich TypeScript erfolgreich.
- Alle **27 URLs** bestehen den automatisierten curl-Test des rohen HTML:
  Status, Sprache, Head-Metadaten, Titel-/Description-Längen, hreflang,
  Canonical, Open Graph, Twitter, Robots, Bildattribute, Hero-Priorität,
  Überschriften und JSON-LD. Sitemap-Abdeckung wird gegen die Praxisdaten
  geprüft; drei falsche Routen zusätzlich auf 404/noindex.
- www-Redirect lokal mit `Host: www.cell-clinics.com` auf
  `/de/network/alpstein?source=seo` geprüft: 301, vollständiges Ziel erhalten.
- JSON-LD lässt sich parsen; notwendige IDs und lokale Eigenschaften geprüft.
  Dies ist **kein** Ersatz für Google Rich Results oder einen vollständigen
  Schema.org-Validator. Fehler-/Warnungsfreiheit dort ist nicht behauptet.
- Vor den nachfolgenden Schutzvorgaben wurden sichtbare Übersetzungstexte gegen
  HEAD verglichen: unverändert. Die anschließende Videohinweis-Anpassung und
  Entfernung der Patienten-Interessenauswahl sind ausdrücklich gewünschte
  Änderungen. `app/globals.css` unverändert. Kein vollständiger Pixelvergleich.
- `git diff --check` erfolgreich.
- Allgemeines `npm run lint`: zwei bereits vorhandene Fehler
  (`react-hooks/set-state-in-effect` in ClinicFinder und SiteHeader) sowie vier
  bestehende Warnungen. Keine Behauptung eines vollständig grünen Lint-Laufs.
  Die SEO-Logik und Prüfdatei bestanden den gezielten Lint-Lauf; am Header
  wurde ausschließlich das Sizes-Attribut ergänzt.

### Lighthouse

Lighthouse 13.4.1, mobile Standardeinstellungen, lokaler Produktions-Build,
Headless Chrome. Einzelmessungen vor der nachfolgenden Video-/Formularanpassung,
keine Garantie für öffentliche Feldwerte.

- Startseite `/de`: zuerst SEO **100**, Performance **89**; nach Anpassung
  der Ladeattribute SEO **100**, Performance **96**, LCP **2,7 s**, CLS **0**.
- Patientenseite `/de/patienten`, finaler Build: SEO **100**, Performance
  **97**, LCP **2,6 s**, CLS **0**.
- Alpstein `/de/network/alpstein`: SEO **100**, Performance **92**, LCP
  **3,4 s**, CLS **0** (gemessen vor der zusätzlichen Praxisbild-Optimierung).
- MEDIVIUM `/de/network/medivium-stuttgart`: zuerst SEO **100**, Performance
  **83**; nach responsiven Sizes und expliziter LCP-Priorität SEO **100**,
  Performance **95**, LCP **2,9 s**, CLS **0**.
- JSON-Berichte liegen lokal unter `/tmp/cell-clinics-seo-*-lighthouse.json`.
- Nicht jede Sprach-/Praxisroute wurde mit Lighthouse vermessen. Vollständige
  Live-Abnahme und Google-Rich-Results-Test stehen nach Freigabe noch aus.

## Nachfolgende Schutzvorgaben

YouTube nur als externer Link hinter lokalem Vorschaubild; keine nachträgliche
iframe-Einbettung. Kein externes CAPTCHA-Skript. Patientenformular auf Name,
E-Mail, Ort und Einwilligung reduziert; zusätzliche medizinische Felder werden
vom strikten API-Schema abgewiesen. Tests versenden keine E-Mails.

Erneuter Produktions-Build und gezielter Lint-Lauf erfolgreich. Alle 27 aktiven
Sprachseiten bestehen auch die ergänzten Schutzprüfungen (keine externen
Scripts/YouTube- oder Google-Maps-Frames im initialen HTML, keine leeren
Schemawerte oder Prüfvermerke, unveränderte Rechtstextlinks). Alle zwölf
vorgesehenen, aber nicht angelegten Therapie-URLs liefern 404 und noindex.
`node scripts/test-patient-inquiry.mjs` prüft das tatsächliche Validierungsschema
ohne Netzwerk-/E-Mail-Versand. Praxisbeschreibungen in DE/EN/ES gegen HEAD
verglichen: unverändert; Praxisdaten und CSS ebenfalls unverändert.

## Referenzen

- Next.js Redirects: https://nextjs.org/docs/app/api-reference/config/next-config-js/redirects
- next-intl Locale-Layout: https://next-intl.dev/docs/routing/setup
- Offizielles Cell-Education-Impressum: https://cell-education.com/impressum
- Offizielle Datenschutzreferenz: https://cell-education.com/en/datenschutz

Keine Änderungen an Search Console, Bing Webmaster Tools, Google Business
Profiles oder externen Partner-Websites vorgenommen.
